import numpy as np

print "\n***Compute stationary solution families***"
# find initial ellipse (continue along epsinv)
sln1 = run(e='linearlc', c='linearlc.1', DS='-')
sln1 = sln1 + run(e='linearlc', c='linearlc.1')

# save output
save(sln1,'linearlc-start')
np.array(sln1.toArray()).dump('./linearlc-start.pkl')

print "\n***Clean the directory***"
cl()
