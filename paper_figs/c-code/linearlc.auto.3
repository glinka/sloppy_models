import numpy as np

# continue down through minimum (continue along contour value)
sln2 = loadbd('linearlc-descent')
sln3 = []
for pt in sln2():
    sln3 = sln3 + run(pt, c='linearlc.3', DS='-')

# save output
save(sln3,'linearlc-fullminus')
np.array(sln3.toArray()).dump('./linearlc-fullminus-largedelta.pkl')

sln3 = []
for pt in sln2():
    sln3 = sln3 + run(pt, c='linearlc.3')

# save output
save(sln3,'linearlc-fullplus')
np.array(sln3.toArray()).dump('./linearlc-fullplus-largedelta.pkl')

print "\n***Clean the directory***"
cl()
