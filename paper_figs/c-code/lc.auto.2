import numpy as np

# continue down through minimum (continue along contour value)
sln1 = loadbd('lc-start')
sln2 = run(sln1('UZ')[0], c='lc.2', DS='-')

# save output
save(sln2,'lc-descent')
np.array(sln2.toArray()).dump('./lc-descent.pkl')

print "\n***Clean the directory***"
cl()
